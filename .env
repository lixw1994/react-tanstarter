# =============================================================================
# Environment Variables Template
# =============================================================================
# This file serves as the complete template for all environment variables.
# Copy to .env.local and fill in your secrets for local development.
#
# Vite loads env files in this order (later files override earlier ones):
#   .env                  ← Base defaults (this file, committed)
#   .env.local            ← Local overrides (gitignored)
#   .env.[mode]           ← Mode-specific (e.g., .env.production)
#   .env.[mode].local     ← Mode-specific local overrides (gitignored)
# =============================================================================

# -----------------------------------------------------------------------------
# Public Variables (VITE_ prefix - exposed to client)
# -----------------------------------------------------------------------------
VITE_BASE_URL=http://localhost:3000
VITE_APP_NAME=Tanstarter
VITE_ALLOW_SIGNUP=true
VITE_ALLOW_PASSWORD_AUTH=true
VITE_ALLOW_GITHUB_AUTH=true
VITE_ALLOW_GOOGLE_AUTH=true
VITE_ALLOW_FEISHU_AUTH=false

# -----------------------------------------------------------------------------
# Server Variables (not exposed to client)
# -----------------------------------------------------------------------------

# Admin configuration - comma-separated list of admin email addresses
ADMIN_EMAILS=

# Better Auth - https://www.better-auth.com/docs/installation
# Generate a secure secret: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# OAuth2 Providers (optional)
# Set callback URL to: {BETTER_AUTH_URL}/api/auth/callback/{provider}
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FEISHU_CLIENT_ID=
FEISHU_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Cloudflare D1 (for drizzle-kit production operations)
# Only needed for db:push:production etc.
# -----------------------------------------------------------------------------
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_DATABASE_ID=
CLOUDFLARE_D1_TOKEN=
