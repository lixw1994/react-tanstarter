# =============================================================================
# Production Environment - Public Variables
# =============================================================================
# This file contains PUBLIC production config that mirrors wrangler.toml [vars].
# Keep this file in sync with wrangler.toml [vars] section.
#
# SECRETS go in .env.production.local (gitignored) for local drizzle operations,
# and are set via `pnpm cf:secret put <KEY>` for Cloudflare Workers.
# =============================================================================

# Public Variables (same as wrangler.toml [vars])
VITE_BASE_URL=https://tanstarter.flybullet.net
VITE_APP_NAME=Tanstarter
VITE_ALLOW_SIGNUP=true
VITE_ALLOW_PASSWORD_AUTH=true
VITE_ALLOW_GITHUB_AUTH=true
VITE_ALLOW_GOOGLE_AUTH=true
VITE_ALLOW_FEISHU_AUTH=false
BETTER_AUTH_URL=https://tanstarter.flybullet.net
